<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.znlccy.house.common.mapper.HouseMapper">

    <!-- 查询所有房屋 -->
    <select id="findAllHouse" resultType="house">
        select * from tb_house
    </select>

    <!-- 通过主键查找房屋 -->
    <select id="findHouseById" parameterType="java.lang.Long" resultType="house">

    </select>

    <!-- 添加房屋 -->
    <insert id="saveHouse" parameterType="house">

    </insert>

    <!-- 更新房屋 -->
    <update id="updateHouse" parameterType="house">

    </update>

    <!-- 通过主键删除房屋 -->
    <delete id="deleteHouseById" parameterType="java.lang.Long">

    </delete>

</mapper>